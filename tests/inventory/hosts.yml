---
all:
  children:
    local:
      hosts:
        ci-localhost:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
    cloud:
      hosts:
        ci-cloud:
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3
