---
- name: Remove podman-docker (conflicts with Docker CE CLI)
  ansible.builtin.dnf:
    name: podman-docker
    state: absent
  failed_when: false

- name: Add Docker CE repo
  ansible.builtin.command: dnf config-manager addrepo --from-repofile https://download.docker.com/linux/fedora/docker-ce.repo
  args:
    creates: /etc/yum.repos.d/docker-ce.repo

- name: Install Python deps for Ansible (requests + packaging + docker)
  ansible.builtin.dnf:
    name:
      - python3-requests
      - python3-packaging
      - python3-docker
    state: present

- name: Install Docker Engine packages
  ansible.builtin.dnf:
    name: "{{ docker_packages }}"
    state: present
    update_cache: true
    disable_gpg_check: true

- name: Enable and start Docker service
  ansible.builtin.systemd:
    name: docker
    enabled: true
    state: started

- name: Ensure docker group exists
  ansible.builtin.group:
    name: docker
    state: present

- name: Add invoking user to docker group
  ansible.builtin.user:
    name: "{{ target_user }}"
    groups: docker
    append: true

- name: Check which Docker images already exist locally
  ansible.builtin.command: "docker image inspect {{ docker_image }}"
  register: docker_image_inspect
  failed_when: false
  changed_when: false
  loop: "{{ docker_images }}"
  loop_control:
    loop_var: docker_image

- name: Pull missing Docker images
  community.docker.docker_image_pull:
    name: "{{ image_check.docker_image }}"
    platform: amd64
  loop: "{{ docker_image_inspect.results }}"
  loop_control:
    loop_var: image_check
  when: image_check.rc != 0