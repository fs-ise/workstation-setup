---
# DNF packages owned by this role; consumed by the package audit.
virtualbox_managed_dnf_packages:
  - VirtualBox
  - VirtualBox-server
  - akmod-VirtualBox
  - virtualbox-guest-additions
  - kernel-devel
  - kernel-headers
