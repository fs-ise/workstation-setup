---
title: "Nemo file manager"
---

Install Nemo and Ptyxis, configure generic thumbnail preferences, and add the `Super+E` shortcut without replacing existing GNOME shortcuts. `Super+E` opens Nemo. In Nemo, pressing F8 opens a separate Ptyxis window running Bash in the current directory. This external terminal window replaces the old embedded `nemo-terminal` extension; the F8 accelerator applies only inside Nemo.

```sh
ansible-playbook -i inventory -K playbooks/lab-stack.yml --tags nemo
```

OCRmyPDF and GROBID continue to own their application-specific Nemo actions; run their tags separately.

::: {.callout-manual}
**🔧 Manual setup and configuration**

If Nemo was already running when its action or layout changed, quit it before reopening it:

```sh
nemo --quit
```

Log out and back in if the desktop session does not immediately show the shortcut or application actions.
:::

::: {.callout-check}
**✅ Check**

```sh
nemo --version
ptyxis --version
dconf read /org/gnome/settings-daemon/plugins/media-keys/custom-keybindings
python -m json.tool ~/.config/nemo/actions-tree.json >/dev/null
```
:::

## Best practices and useful links

- [Nemo project](https://github.com/linuxmint/nemo)
- [OCR setup](ocr.html)
- [GROBID setup](grobid.html)
